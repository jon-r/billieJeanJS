// options
export default {
  selector: '[data-gridme]',
  gridLines: 5,
  rectSize: 2, // including 1px pad
  spawnSpeed: 200, // time per dot spawn
  runSpeed: 40, //time per dot activate the next dot
};

// export default opts;
